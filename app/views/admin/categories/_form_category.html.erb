<%= form_for [:admin, @category] do |f| %>
  <div class="form-group">
    <%= f.label :name, class: "notice" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit t("create"), class: "btn btn-primary" %>
  </div>
<% end %>
