<tr>
  <td><%= order_counter + 1 %></td>
  <td><%= order.name %></td>
  <td><%= order.email %></td>
  <td><%= order.phone %></td>
  <td><%= order.address %></td>
  <td><%= order.status %></td>
  <td><%= order.created_at.strftime Settings.format_time %></td>
  <td align="center">
    <%= link_to t("handling"), edit_admin_order_path(order),
      class: "btn btn-sm grey-salsa btn-outline" %>
  </td>
</tr>
